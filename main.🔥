from collections import List

fn main():
    var lists: List[Int] = List(2,3,4,5) 
    lists.append(7)
    lists.append(9)
    print("poping out ", lists.pop())
    for idx in range(len(lists)):
        print(lists[idx])
    var l2 = List(1,2,3,4,5)
    for x in l2:
        print(x[])

